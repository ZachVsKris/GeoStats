pyshp==2.3.1
shapely==2.1.1
pyproj==3.7.2
# Köppen-Geiger raster importer/tests require NumPy + Rasterio.
# Keep ranges broad enough for Python 3.12 wheels while remaining below breaking major versions.
numpy>=1.26,<3
rasterio>=1.3,<2
