# GeoStats v15.1.0

Start with `START_HERE_V15.md`.

The required Supabase installer is `RUN_THIS_IN_SUPABASE_FOR_V15.sql`.

After deployment, the new catalog workflow is at:

```text
/admin/review
```

Upload everything inside this folder to the root of the GitHub repository. Keep the hidden `.github` folder; the visible `GITHUB_ACTIONS/workflows` folder remains a backup for manual GitHub uploads.
