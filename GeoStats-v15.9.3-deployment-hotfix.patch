diff --git a/.github/workflows/verify-v15.yml b/.github/workflows/verify-v15.yml
index bcce81ed675d1a9b0c624100f004deead9e8f78f..206f0351a314012b103b39e4f9880a6133274c0d 100644
--- a/.github/workflows/verify-v15.yml
+++ b/.github/workflows/verify-v15.yml
@@ -1,4 +1,4 @@
-name: Verify GeoStats v15.9.2
+name: Verify GeoStats v15.9.3
 
 on:
   workflow_dispatch:
@@ -33,7 +33,7 @@ jobs:
             -r scripts/requirements-data-importers.txt \
             -r scripts/requirements-faostat.txt \
             -r scripts/requirements-geography.txt
-      - name: Verify v15.9.2 repository, generators, automatic importers, and review rules
+      - name: Verify v15.9.3 repository, generators, automatic importers, and review rules
         run: npm run test-v15-9
       - name: Type-check the complete application
         run: npm run typecheck
diff --git a/ROLLBACK_V15_9_2.sql b/ROLLBACK_V15_9_2.sql
index 2106ac8f6b0a4103cad42aa0a0c0fc9039d63676..b896e2f5ae21ea2830a25bb836606edc440b6805 100644
--- a/ROLLBACK_V15_9_2.sql
+++ b/ROLLBACK_V15_9_2.sql
@@ -1,18 +1,19 @@
--- GeoStats v15.9.2 safe database rollback.
--- v15.9.2 is additive. Older application versions ignore these columns, so the
--- safest rollback preserves the version history rather than deleting it.
+-- GeoStats v15.9.2 score-version compatibility rollback.
+-- This does not undo the broader v15.9/v15.9.1 catalog migrations. It keeps
+-- the additive version columns and restores defaults that remain compatible
+-- with an older application that does not explicitly supply those columns.
 
 begin;
 select pg_advisory_xact_lock(hashtext('geostats-v15.9.2-score-versioning'));
 
 alter table if exists public.daily_scores
-  alter column scoring_version drop default,
-  alter column board_normalization_version drop default,
-  alter column leaderboard_rating_version drop default,
-  alter column rules_version drop default,
-  alter column category_set_version drop default,
-  alter column dataset_version drop default;
+  alter column scoring_version set default 'placements-pre-v15.9.2',
+  alter column board_normalization_version set default 'daily-distribution-z-v1',
+  alter column leaderboard_rating_version set default 'board-relative-bayesian-v1',
+  alter column rules_version set default 'legacy-unknown',
+  alter column category_set_version set default 'legacy-unknown',
+  alter column dataset_version set default 'legacy-unknown';
 
 commit;
 
-select 'Version metadata columns were preserved; deploy the prior application version to complete rollback.' as result;
+select 'Score-version defaults are compatible with the prior application. Catalog and board changes were not rolled back.' as result;
diff --git a/app/api/admin/category-review/route.ts b/app/api/admin/category-review/route.ts
index a6e5e65364cbc49f17e192673d8662b058508c24..05f8d4a7e7c3dea0b647b002cc08cc72c3857de6 100644
--- a/app/api/admin/category-review/route.ts
+++ b/app/api/admin/category-review/route.ts
@@ -89,7 +89,7 @@ export async function GET(request: Request) {
     return NextResponse.json(
       {
         error: migrationMissing
-          ? "Run RUN_THIS_IN_SUPABASE_FOR_V15_9.sql before using the Category Review Workbench."
+          ? "Run RUN_THIS_IN_SUPABASE_FOR_V15_9_2.sql before using the Category Review Workbench."
           : error.message,
         migrationApplied: !migrationMissing,
       },
diff --git a/app/api/scores/route.ts b/app/api/scores/route.ts
index 5f9579bcddc31d67b4b9bf98034be410633b3b98..e7db48551826de323825bc34fe99398ceef2f641 100644
--- a/app/api/scores/route.ts
+++ b/app/api/scores/route.ts
@@ -78,13 +78,15 @@ export async function GET(request: Request) {
     return NextResponse.json({ error: "Invalid challenge date." }, { status: 400 });
   }
 
-  let { data, error } = await admin
+  const versionedResult = await admin
     .from("daily_scores")
     .select(scoreColumns)
     .eq("user_id", user.id)
     .eq("challenge_date", challengeDate)
     .eq("difficulty", difficulty)
     .maybeSingle();
+  let data: unknown = versionedResult.data;
+  let error = versionedResult.error;
   if (missingVersionColumns(error)) {
     const legacy = await admin
       .from("daily_scores")
@@ -121,13 +123,15 @@ export async function POST(request: Request) {
     return NextResponse.json({ error: "Invalid score submission." }, { status: 400 });
   }
 
-  let { data: existing, error: existingError } = await admin
+  const versionedExisting = await admin
     .from("daily_scores")
     .select(scoreColumns)
     .eq("user_id", user.id)
     .eq("challenge_date", body.challengeDate)
     .eq("difficulty", difficulty)
     .maybeSingle();
+  let existing: unknown = versionedExisting.data;
+  let existingError = versionedExisting.error;
   if (missingVersionColumns(existingError)) {
     const legacy = await admin
       .from("daily_scores")
@@ -204,9 +208,11 @@ export async function POST(request: Request) {
       category_set_version: (challenge as StoredChallenge).category_set_version,
       dataset_version: (challenge as StoredChallenge).dataset_version,
     };
-    let { data, error } = await admin.from("daily_scores").insert(versionedPayload)
+    const versionedInsert = await admin.from("daily_scores").insert(versionedPayload)
       .select(scoreColumns)
       .single();
+    let data: unknown = versionedInsert.data;
+    let error = versionedInsert.error;
     if (missingVersionColumns(error)) {
       const legacyInsert = await admin.from("daily_scores").insert(legacyPayload)
         .select(legacyScoreColumns)
@@ -216,23 +222,28 @@ export async function POST(request: Request) {
     }
 
     if (error?.code === "23505") {
-      let savedResult = await admin
+      const versionedSaved = await admin
         .from("daily_scores")
         .select(scoreColumns)
         .eq("user_id", user.id)
         .eq("challenge_date", body.challengeDate)
         .eq("difficulty", difficulty)
         .single();
-      if (missingVersionColumns(savedResult.error)) {
-        savedResult = await admin
+      let savedData: unknown = versionedSaved.data;
+      let savedError = versionedSaved.error;
+      if (missingVersionColumns(savedError)) {
+        const legacySaved = await admin
           .from("daily_scores")
           .select(legacyScoreColumns)
           .eq("user_id", user.id)
           .eq("challenge_date", body.challengeDate)
           .eq("difficulty", difficulty)
           .single();
+        savedData = legacySaved.data;
+        savedError = legacySaved.error;
       }
-      return NextResponse.json({ saved: true, alreadyCompleted: true, result: savedResult.data }, {
+      if (savedError) return NextResponse.json({ error: savedError.message }, { status: 500 });
+      return NextResponse.json({ saved: true, alreadyCompleted: true, result: savedData }, {
         headers: { "Cache-Control": "private, no-store" },
       });
     }
diff --git a/lib/version.ts b/lib/version.ts
index df5a830cb0bdf297ab5da3d4841b9087ae3c4b21..8736cc4b68da88dba63e49f651a0aade7c8b369c 100644
--- a/lib/version.ts
+++ b/lib/version.ts
@@ -1,4 +1,4 @@
-export const APP_VERSION = "15.9.2";
+export const APP_VERSION = "15.9.3";
 export const DATASET_VERSION = "2026-07-30-automatic-culture-consumption-geography-integrity-v15.9.2";
 export const RULES_VERSION = "15.9.2";
 export const CATEGORY_SET_VERSION = "SCOUT-ADVENTURER-EXPERT-V15-9-2-PRESERVE-VALID-MODES";
diff --git a/package.json b/package.json
index dd1877a3e5591e3a867183fb41ccd89acee287c9..c11cba8c2c1d1db9b5ac9f15a2b32da2fc69d65d 100644
--- a/package.json
+++ b/package.json
@@ -1,6 +1,6 @@
 {
   "name": "geostats",
-  "version": "15.9.2",
+  "version": "15.9.3",
   "private": true,
   "scripts": {
     "dev": "next dev",
diff --git a/scripts/test-v15-9-static.cjs b/scripts/test-v15-9-static.cjs
index 56416c7564d70ac940c80cd6873d40e77d1af603..386dab9e23247dbf9b98b65632a060ce89bdd410 100644
--- a/scripts/test-v15-9-static.cjs
+++ b/scripts/test-v15-9-static.cjs
@@ -58,6 +58,13 @@ const proxy = read("proxy.ts");
 requireCheck(proxy.includes('"/seeded/expert": "/random/expert"') && proxy.includes("request.nextUrl.clone()"), "legacy Seeded redirects do not preserve Random paths and query parameters");
 const scoreRoute = read("app/api/scores/route.ts");
 requireCheck(scoreRoute.includes("SCORING_VERSION") && scoreRoute.includes("board_normalization_version") && scoreRoute.includes("leaderboard_rating_version"), "score/rating versions are not stored at submission");
+requireCheck(scoreRoute.includes("let data: unknown = versionedResult.data") && scoreRoute.includes("let existing: unknown = versionedExisting.data") && scoreRoute.includes("let savedData: unknown = versionedSaved.data"), "score-route legacy fallbacks do not normalize incompatible Supabase result shapes");
+requireCheck(!scoreRoute.includes("let { data, error } = await admin") && !scoreRoute.includes("let savedResult = await admin"), "score-route fallback still reassigns incompatible inferred Supabase result types");
+requireCheck(scoreRoute.includes("if (savedError) return NextResponse.json"), "duplicate-score recovery can still report success after a failed lookup");
+const categoryReviewRoute = read("app/api/admin/category-review/route.ts");
+requireCheck(categoryReviewRoute.includes("RUN_THIS_IN_SUPABASE_FOR_V15_9_2.sql") && !categoryReviewRoute.includes("RUN_THIS_IN_SUPABASE_FOR_V15_9.sql before"), "category-review migration instruction is stale");
+const rollback = read("ROLLBACK_V15_9_2.sql");
+requireCheck(rollback.includes("set default 'placements-pre-v15.9.2'") && !rollback.includes("drop default"), "score-version rollback can still break legacy score inserts");
 const leaderboardRoute = read("app/api/leaderboard/route.ts");
 requireCheck(leaderboardRoute.includes("LEADERBOARD_RATING_VERSION") && leaderboardRoute.includes("BOARD_NORMALIZATION_VERSION"), "leaderboard response does not disclose its versioned method");
 requireCheck(component.includes("observationReference") && component.includes("Why this rank?"), "static reference labels or rank explanation are missing from results");
