# Visible GitHub Actions copy

GitHub requires workflow files at `.github/workflows/`, which is hidden in macOS Finder.
This visible folder contains an identical copy for inspection and manual replacement.

For normal deployment, upload the complete repository as provided: the real `.github/workflows/`
folder is already included. If manually copying files, place the contents of this folder into
`.github/workflows/` in GitHub.
