console.log(`GeoStats v14.4 seed-capacity diagnostics are available at:
  /api/admin/daily/capacity

The endpoint returns exact raw n-choose-k category counts and deterministic
estimates after GeoStats diversity rules for Scout, Adventurer, and Expert.
Full country-bank capacity is not presented as an exact number
because valid countries depend on the chosen category set; generation-health
sampling is the more useful measure.`);
